Gentle Pause — V15 PWA BETA v4

LOCKED MASTER SHA-256:
fdd5f4eec19081d3e36a1f701dcdccdfc5ba18caee9480a931cdb39d4c259872

BASE:
PWA Beta v2 (clean baseline; ineffective v3 override is NOT carried forward).

ONLY LAYOUT CHANGE:
- Mobile installed standalone PWA only:
  .stage and .screen use 100vh instead of the V15 100dvh constraint.
- Reason: iOS/WebKit installed-PWA viewport quirk can make 100dvh exclude the
  bottom safe-area, creating excess apparent space below the product UI.
- Existing .frame padding with env(safe-area-inset-bottom) remains unchanged,
  so the Home Indicator remains protected.

UNCHANGED:
- Safari/browser V15 CSS and geometry
- card/button styling and spacing rules
- 30 Pause texts/order
- Welcome / Today / Pause / Tomorrow / Final workflow
- local calendar logic
- localStorage progress
- navigation
- typography/background
- approved icons
- manifest
- desktop CSS

SERVICE WORKER:
- cache name beta-v2 -> beta-v4 only.
